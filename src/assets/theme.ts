import {
  borderRadius,
  boxShadow,
  fontSize,
  height,
  letterSpacing,
  margin,
  position,
  svgSizes,
  width,
} from '@constants';
import { ITheme } from '@interfaces';

export const theme: ITheme = {
  fontSize,
  width,
  height,
  borderRadius,
  boxShadow,
  margin,
  svgSizes,
  position,
  letterSpacing,
};
